/* global markovChain, describe, it, expect, should */

describe('markovChain()', function () {
  'use strict';

  it('exists', function () {
    expect(markovChain).to.be.a('function');

  });
  
});
